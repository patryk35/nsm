package pdm.networkservicesmonitor.agent.worker;

public class ProxyWorker {
    //Controller gets request and add it to queue than worker loads from this queue
    // TODO(critical):  Copy-Paste code from monitor and send like in ConnectionWorker. Child agent don't know about problems with connection proxy agent - monitor
}
